FROM node:20-bullseye

WORKDIR /app

# 複製 package 檔案
COPY package*.json ./

# 安裝依賴（包括 devDependencies）
RUN npm install --verbose

# 複製所有源代碼
COPY . .

# 暴露端口
EXPOSE 3000

# 直接運行 npm run dev
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]