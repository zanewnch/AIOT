# Drone WebSocket 微服務 Kubernetes 配置
apiVersion: apps/v1
kind: Deployment
metadata:
  name: drone-websocket-service-deployment
  namespace: aiot
  labels:
    app: drone-websocket-service
    app.kubernetes.io/name: drone-websocket-service
    app.kubernetes.io/component: microservice
    app.kubernetes.io/part-of: aiot-microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: drone-websocket-service
  template:
    metadata:
      labels:
        app: drone-websocket-service
    spec:
      containers:
      - name: drone-websocket-service
        image: aiot-drone-websocket-service:latest
        imagePullPolicy: Never  # 使用本地構建的鏡像
        ports:
        - containerPort: 3004
          name: websocket
        env:
        - name: SERVICE_NAME
          valueFrom:
            configMapKeyRef:
              name: aiot-microservice-config
              key: DRONE_WEBSOCKET_SERVICE_NAME
        - name: SERVICE_PORT
          valueFrom:
            configMapKeyRef:
              name: aiot-microservice-config
              key: DRONE_WEBSOCKET_SERVICE_PORT
        - name: CORS_ORIGIN
          valueFrom:
            configMapKeyRef:
              name: aiot-microservice-config
              key: CORS_ORIGIN
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: aiot-microservice-config
              key: DRONE_DB_NAME
        # 從公共配置引入環境變數
        envFrom:
        - configMapRef:
            name: aiot-common-config
        - secretRef:
            name: aiot-database-secrets
        - secretRef:
            name: aiot-app-secrets
        # 開發模式：掛載源代碼進行 hot-reload
        volumeMounts:
        - name: drone-websocket-source
          mountPath: /app
        livenessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "400m"
      volumes:
      - name: drone-websocket-source
        hostPath:
          path: /home/user/GitHub/AIOT/microServices/drone-websocket-service
          type: Directory
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: drone-websocket-service
  namespace: aiot
  labels:
    app: drone-websocket-service
    app.kubernetes.io/name: drone-websocket-service
    app.kubernetes.io/component: microservice
spec:
  selector:
    app: drone-websocket-service
  ports:
  - port: 3004
    targetPort: 3004
    protocol: TCP
    name: websocket
  type: ClusterIP