# General 微服務 Kubernetes 配置
apiVersion: apps/v1
kind: Deployment
metadata:
  name: general-service-deployment
  namespace: aiot
  labels:
    app: general-service
    app.kubernetes.io/name: general-service
    app.kubernetes.io/component: microservice
    app.kubernetes.io/part-of: aiot-microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: general-service
  template:
    metadata:
      labels:
        app: general-service
    spec:
      containers:
      - name: general-service
        image: node:18-bullseye  # 使用基礎鏡像
        command: ['sh', '-c', 'mkdir -p src && echo "console.log(\"General Service starting in development mode\"); process.exit(0);" > src/server.ts && npm install && npm run dev']
        ports:
        - containerPort: 50053
          name: grpc
        env:
        - name: SERVICE_NAME
          valueFrom:
            configMapKeyRef:
              name: aiot-microservice-config
              key: GENERAL_SERVICE_NAME
        - name: GRPC_PORT
          valueFrom:
            configMapKeyRef:
              name: aiot-microservice-config
              key: GENERAL_GRPC_PORT
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: aiot-microservice-config
              key: GENERAL_DB_NAME
        # 從公共配置引入環境變數
        envFrom:
        - configMapRef:
            name: aiot-common-config
        - configMapRef:
            name: dev-environment-config
        - secretRef:
            name: aiot-database-secrets
        - secretRef:
            name: aiot-app-secrets
        # 開發模式：ConfigMap + emptyDir 實現 hot-reload
        volumeMounts:
        - name: app-workspace
          mountPath: /app
        - name: source-files
          mountPath: /app/package.json
          subPath: package.json
        - name: source-files
          mountPath: /app/nodemon.json
          subPath: nodemon.json
        - name: source-files
          mountPath: /app/tsconfig.json
          subPath: tsconfig.json
        livenessProbe:
          tcpSocket:
            port: 50053
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 50053
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "400m"
      volumes:
      - name: app-workspace
        emptyDir: {}
      - name: source-files
        configMap:
          name: nodejs-service-template
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: general-service
  namespace: aiot
  labels:
    app: general-service
    app.kubernetes.io/name: general-service
    app.kubernetes.io/component: microservice
spec:
  selector:
    app: general-service
  ports:
  - port: 50053
    targetPort: 50053
    protocol: TCP
    name: grpc
  type: ClusterIP