# General 微服務 Kubernetes 配置
apiVersion: apps/v1
kind: Deployment
metadata:
  name: general-service-deployment
  namespace: aiot
  labels:
    app: general-service
    app.kubernetes.io/name: general-service
    app.kubernetes.io/component: microservice
    app.kubernetes.io/part-of: aiot-microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: general-service
  template:
    metadata:
      labels:
        app: general-service
    spec:
      containers:
      - name: general-service
        image: aiot-general-service:latest
        imagePullPolicy: Never  # 使用本地構建的鏡像
        ports:
        - containerPort: 50053
          name: grpc
        env:
        - name: SERVICE_NAME
          valueFrom:
            configMapKeyRef:
              name: aiot-microservice-config
              key: GENERAL_SERVICE_NAME
        - name: GRPC_PORT
          valueFrom:
            configMapKeyRef:
              name: aiot-microservice-config
              key: GENERAL_GRPC_PORT
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: aiot-microservice-config
              key: GENERAL_DB_NAME
        # 從公共配置引入環境變數
        envFrom:
        - configMapRef:
            name: aiot-common-config
        - secretRef:
            name: aiot-database-secrets
        - secretRef:
            name: aiot-app-secrets
        # 開發模式：掛載源代碼進行 hot-reload
        volumeMounts:
        - name: general-source
          mountPath: /app
        livenessProbe:
          tcpSocket:
            port: 50053
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 50053
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "400m"
      volumes:
      - name: general-source
        hostPath:
          path: /home/user/GitHub/AIOT/microServices/general-service
          type: Directory
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: general-service
  namespace: aiot
  labels:
    app: general-service
    app.kubernetes.io/name: general-service
    app.kubernetes.io/component: microservice
spec:
  selector:
    app: general-service
  ports:
  - port: 50053
    targetPort: 50053
    protocol: TCP
    name: grpc
  type: ClusterIP