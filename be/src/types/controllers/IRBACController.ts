/**
 * RBAC 控制器介面定義
 * 
 * 定義 RBAC 主控制器必須實現的基本結構。
 * RBAC 控制器負責整合所有角色權限相關的子控制器業務邏輯。
 * 
 * @module Types
 */

export interface IRBACController {
    // Currently empty as this controller serves as a container for sub-controllers
    // Future business logic methods can be added here as needed
}