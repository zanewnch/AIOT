# ================================================
# 通知系統環境配置範例
# ================================================
# 複製此文件為 .env 並根據您的環境修改配置值

# ================================================
# Email 通知配置
# ================================================

# SMTP 伺服器設定
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# SMTP 認證資訊
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# 發件人資訊
SMTP_FROM_NAME=AIOT 排程服務
SMTP_FROM_EMAIL=noreply@yourdomain.com

# 預設收件人 (用逗號分隔)
NOTIFICATION_DEFAULT_EMAILS=admin@yourdomain.com,devops@yourdomain.com

# 是否啟用 Email 通知
EMAIL_NOTIFICATIONS_ENABLED=true

# ================================================
# Webhook 通知配置
# ================================================

# Webhook URL (例如 Slack Webhook)
WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# 請求超時時間 (毫秒)
WEBHOOK_TIMEOUT=30000

# 是否驗證 SSL 憑證
WEBHOOK_VERIFY_SSL=true

# 重試配置
WEBHOOK_RETRY_ATTEMPTS=3
WEBHOOK_RETRY_DELAY=1000

# 是否啟用 Webhook 通知
WEBHOOK_NOTIFICATIONS_ENABLED=true

# ================================================
# 通知系統一般配置
# ================================================

# 最大重試次數
NOTIFICATION_MAX_RETRIES=3

# 重試延遲 (毫秒)
NOTIFICATION_RETRY_DELAY=5000

# 請求超時 (毫秒)
NOTIFICATION_TIMEOUT=30000

# ================================================
# Redis 配置 (用於通知佇列和狀態儲存)
# ================================================

REDIS_URL=redis://localhost:6379

# ================================================
# 環境設定
# ================================================

NODE_ENV=development

# ================================================
# 配置說明
# ================================================

# Gmail SMTP 配置說明:
# 1. 啟用兩步驟驗證
# 2. 生成應用程式密碼
# 3. 使用應用程式密碼作為 SMTP_PASSWORD

# Slack Webhook 設定說明:
# 1. 進入 Slack App 設定頁面
# 2. 創建新的 Incoming Webhook
# 3. 選擇要發送訊息的頻道
# 4. 複製 Webhook URL

# 其他常用 SMTP 設定:

# Outlook/Hotmail:
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false

# Yahoo Mail:
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=465
# SMTP_SECURE=true

# 自訂 SMTP 伺服器:
# SMTP_HOST=mail.yourdomain.com
# SMTP_PORT=587
# SMTP_SECURE=false