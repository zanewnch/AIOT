# ==================================================
# Archive Consumer Service - Development Dockerfile
# ==================================================

FROM node:18-alpine

# 設定工作目錄
WORKDIR /app

# 安裝系統依賴
RUN apk add --no-cache curl netcat-openbsd git

# 複製源代碼（包含 package.json）
COPY microServices/archive-consumer-service .

# 安裝 shared-packages 從 GitHub
RUN npm cache clean --force && npm install git+https://github.com/zanewnch/aiot-shared-packages.git --no-package-lock --include=dev

# 建立日誌目錄
RUN mkdir -p /app/logs

# 設定環境變數
ENV NODE_ENV=development

# 暴露端口
EXPOSE 3005

# 啟動命令 - 使用 ts-node-dev 支援 hot reload
CMD ["npm", "run", "dev"]