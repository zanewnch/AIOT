# AIOT LLM Service - Development Dockerfile
# 🤖 開發環境專用，支援 Django runserver + Hot-Reload 和除錯

FROM python:3.11-slim as development

# 設定工作目錄
WORKDIR /app

# 安裝系統依賴
RUN apt-get update && \
    apt-get install -y \
    curl \
    netcat-traditional \
    gcc \
    g++ \
    && rm -rf /var/lib/apt/lists/*

# 複製需求文件並安裝 Python 依賴
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

# 安裝開發專用依賴
RUN pip install debugpy ipdb django-debug-toolbar

# 創建非 root 用戶 (安全最佳實踐)
RUN groupadd -g 1001 djangouser && useradd -r -u 1001 -g djangouser djangouser
RUN chown -R djangouser:djangouser /app
USER djangouser

# 開發端口 (Django + Debug)
EXPOSE 8020 5678

# 健康檢查
HEALTHCHECK --interval=30s --timeout=10s --start-period=10s --retries=3 \
  CMD curl -f http://localhost:8020/api/health/ || exit 1

# 開發環境啟動命令 (Django runserver 自帶 auto-reload)
CMD ["python", "manage.py", "runserver", "0.0.0.0:8020"]